language: node_js

node_js:
  - '14'

services:
  - docker

before_script:
  - npm install
  - chmod +x ./scripts/docker.sh

cache:
  directories:
    - "node_modules"

script: npm test

after_success:
  - ./scripts/docker.sh

notifications:
  email:  false
