language: node_js

node_js:
  - '14'

services:
  - docker

before_script:
  - npm install

cache:
  directories:
    - "node_modules"

script: npm test

after_success:
  - chmod +x ./scripts/deploy.sh

notifications:
  email:  false
